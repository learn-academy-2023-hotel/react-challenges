const BadBot = (props) => {
    
    return (
        <>
     {/* first index, every 3rd = B */}
     {/*  */}
     {/* second index, every 3rd = L
     third index, every 3rd = A */}
        
     {/*for (let i=0; word.length; i+3)  */}
     {/* /*for (let i=1; word.length, i+3)  */}
        {/* return b[i] */} */}
        </>
    )
}
export default BadBot